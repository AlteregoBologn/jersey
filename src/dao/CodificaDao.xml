<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.CodificaDao">

	<sql id="orderBy">
		order by DESCRIZIONE
	</sql>

	<sql id="condition">
		WHERE 1=1
		<if test="descrizioneLike != null ">
			AND DESCRIZIONE like #{descrizioneLike}||'%'
		</if>
		<if test="descrizione != null ">
			AND DESCRIZIONE = #{descrizione}
		</if>
		<if test="tipo != null ">
			AND TIPO = #{tipo}
		</if>
		<if test="unid != null ">
			AND UNID = #{unid}
		</if>
	</sql>

	<insert id="insert">
		insert into T_CODIFICA (
		DESCRIZIONE,
		TIPO,
		UNID)
		VALUES (
		#{descrizione},
		#{tipo},
		#{unid})
	</insert>

	<select id="count" resultType="Integer">
		SELECT count(0) from T_CODIFICA
		<include refid="condition" />
	</select>

	<update id="update">
		update T_CODIFICA set
		DESCRIZIONE = #{descrizione}
		WHERE 
	 	UNID = #{unid} AND TIPO = #{tipo}
	</update>

	<select id="loadAll" resultType="model.Codifica">
		SELECT * from T_CODIFICA
		<include refid="condition" />
		<include refid="orderBy" />
	</select>

	<delete id="delete">
		delete from T_CODIFICA
		WHERE UNID = #{unid} AND TIPO = #{tipo}
	</delete>

</mapper>